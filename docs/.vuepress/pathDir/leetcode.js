/**
 * Leetcode 目录配置
 */
 const baseDir = require('./root')

 const root = baseDir + 'leetcode/'
 const valid_bracket = root + '有效的括号'
 const sum_of_two_number = root + '两数之和'
 const add_two_number_together = root + '两数相加'
 const the_longest_string_without_repeating_characters = root + '无重复字符的最长子串'
 const the_longest_return_substring = root + '最长回文子串'
 const integer_inversion = root + '整数反转'
 const palindromic_number = root + '回文数'
 const roman_numerals_to_whole_numbers = root + '罗马数字转整数'
 const search_rotationally_sorted_array = "搜索旋转排序数组"

 module.exports = {
    root,
    valid_bracket,
    sum_of_two_number,
    add_two_number_together,
    the_longest_string_without_repeating_characters,
    the_longest_return_substring,
    integer_inversion,
    palindromic_number,
    roman_numerals_to_whole_numbers,
    search_rotationally_sorted_array,
 }